---const testimonials = [

// Minimal valid Astro component restored  {

---    id: 1,

    name: "Sarah Johnson",

<section>    role: "Small Business Owner",

  <h2>Testimonials Carousel Component Restored</h2>    location: "Austin, TX",

  <p>This component is now clean and ready for incremental feature restoration.</p>---

</section>

<section>
  <h2>What Our Customers Say</h2>
  <ul>
    {testimonials.map(testimonial => (
      <li>
        <strong>{testimonial.name}</strong> ({testimonial.role}, {testimonial.location}):<br />
        {testimonial.text} <br />
        <em>Savings: {testimonial.savings}</em>
      </li>
    ))}
  </ul>
</section>
                {testimonial.text} <br />
                <em>Savings: {testimonial.savings}</em>
              </li>
            ))}
          </ul>
        </section>
            <em>Savings: {testimonial.savings}</em>
          </li>
        ))}
      </ul>
    </div>

                    Saved {testimonial.savings}    insuranceType: "Life Insurance",  {

                  </div>

                </div>    verified: true    id: 2,

              </div>

            </div>  }    name: "Michael Chen",

          ))}

        </div>];    role: "Software Engineer",

      </div>

---    location: "Seattle, WA", 

      <button id="prevBtn" class="absolute left-4 top-1/2 transform -translate-y-1/2 w-12 h-12 bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm rounded-full shadow-lg flex items-center justify-center text-gray-600 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 transition-all duration-300">

        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">    avatar: "/images/avatars/michael-c.jpg",

          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>

        </svg><section class="relative py-16 overflow-hidden bg-gradient-to-b from-white to-gray-50 dark:from-gray-900 dark:to-gray-800">    rating: 5,

      const testimonials = [

            <span class="sr-only">Go to testimonial {index + 1}</span>    <div class="text-center mb-12">    name: "Jennifer Martinez",

          </button>

        ))}      <h2 class="text-4xl font-bold text-gray-900 dark:text-white mb-4">    role: "Marketing Director",

      </div>

    </div>        What Our Customers Say    location: "Miami, FL",

  </div>

</div>      </h2>    avatar: "/images/avatars/jennifer-m.jpg",



<style>      <p class="text-lg text-gray-600 dark:text-gray-300">    rating: 5,

  .pagination-dot {

    width: 44px;        Join thousands of satisfied customers who found their perfect insurance match    text: "After buying our first home, we were overwhelmed by insurance options. Sezarr's step-by-step guide and instant quotes made it simple. We got excellent coverage and saved $315 compared to our realtor's recommendation.",

    height: 44px;

    border-radius: 50%;      </p>    savings: "$315",

    background: #e5e7eb;
    border: none;    </div>    insuranceType: "Homeowners Insurance",

    cursor: pointer;

    transition: all 0.3s ease;    verified: true

  }

    <!-- Testimonials Carousel -->  },

  .pagination-dot:hover {

    transform: scale(1.1);    <div class="relative max-w-4xl mx-auto">  {

  }

      <div id="testimonialCarousel" class="overflow-hidden">    id: 4,

  .pagination-dot.active {

    background: linear-gradient(45deg, #3B82F6, #8B5CF6);        <div class="flex transition-transform duration-700 ease-in-out" id="testimonialTrack">    name: "David Thompson",

    transform: scale(1.25);

  }          {testimonials.map((testimonial, index) => (    role: "Financial Advisor",



  @media (max-width: 768px) {            <div class="w-full flex-shrink-0 px-4" data-testimonial={index}>    location: "Denver, CO",

    #prevBtn,

    #nextBtn {              <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-xl p-8 relative overflow-hidden">    avatar: "/images/avatars/david-t.jpg",

      display: none;

    }                <div class="relative z-10">    rating: 5,

    

    #testimonialCarousel {                  <div class="flex justify-between items-start mb-6">    text: "I recommend Sezarr to all my clients for life insurance planning. Their calculators are incredibly accurate, and the educational content helps people make informed decisions. The term life quotes are consistently competitive.",

      padding: 0 1rem;

    }                    <!-- Rating Stars -->    savings: "$580",

  }

</style>                    <div class="flex items-center">    insuranceType: "Life Insurance",



<script>                      <div class="flex gap-1">    verified: true

  document.addEventListener('DOMContentLoaded', () => {

    let currentIndex = 0;                        {Array.from({ length: testimonial.rating }).map((_, i) => (  },

    const track = document.getElementById('testimonialTrack');

    const dots = document.querySelectorAll('.pagination-dot');                          <svg class="w-5 h-5 text-yellow-400 animate-star-glow" fill="currentColor" viewBox="0 0 20 20">  {

    const prevBtn = document.getElementById('prevBtn');

    const nextBtn = document.getElementById('nextBtn');                            <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>    id: 5,

    const totalSlides = dots.length;

                          </svg>    name: "Amanda Rodriguez",

    function updateSlidePosition() {

      if (track) {                        ))}    role: "Freelance Designer",

        track.style.transform = `translateX(-${currentIndex * 100}%)`;

        dots.forEach((dot, index) => {                      </div>    location: "Portland, OR",

          dot.classList.toggle('active', index === currentIndex);

        });                    </div>    avatar: "/images/avatars/amanda-r.jpg", 

      }

    }                  </div>    rating: 5,



    function goToSlide(index) {    text: "Switching car insurance used to be such a hassle. Sezarr's platform compared 15+ providers instantly and found me coverage with better benefits for $426 less per year. The whole process took less than 10 minutes.",

      currentIndex = Math.max(0, Math.min(index, totalSlides - 1));

      updateSlidePosition();                  <!-- Testimonial Text -->    savings: "$426",

    }

                  <blockquote class="text-gray-700 dark:text-gray-300 mb-6 text-lg leading-relaxed">    insuranceType: "Auto Insurance",

    function nextSlide() {

      currentIndex = (currentIndex + 1) % totalSlides;                    "{testimonial.text}"    verified: true

      updateSlidePosition();

    }                  </blockquote>  },



    function prevSlide() {  {

      currentIndex = (currentIndex - 1 + totalSlides) % totalSlides;

      updateSlidePosition();                  <!-- Author Info -->    id: 6,

    }

                  <div class="flex items-center gap-4">    name: "Robert Kim",

    // Event Listeners

    if (prevBtn) prevBtn.addEventListener('click', prevSlide);                    <div class="relative">    role: "Restaurant Owner",

    if (nextBtn) nextBtn.addEventListener('click', nextSlide);

                      {testimonial.avatar ? (    location: "Chicago, IL",

    dots.forEach((dot, index) => {

      dot.addEventListener('click', () => goToSlide(index));                        <img src={testimonial.avatar} alt={testimonial.name} class="w-12 h-12 rounded-full" loading="lazy" />    avatar: "/images/avatars/robert-k.jpg",

    });

                      ) : (    rating: 5,

    // Initialize carousel

    updateSlidePosition();                        <div class="w-12 h-12 rounded-full bg-gradient-to-br from-blue-500 to-purple-500 flex items-center justify-center text-white text-lg font-semibold">    text: "Running a restaurant requires multiple insurance policies. Sezarr's business insurance section helped me understand exactly what I needed and connected me with agents who specialize in food service. Excellent experience!",



    // Auto-advance slides every 5 seconds if not hovering                          {testimonial.name.split(' ').map(n => n[0]).join('')}    savings: "$920",

    let autoAdvance;

    const startAutoAdvance = () => {                        </div>

      autoAdvance = setInterval(nextSlide, 5000);

    };                      )}    insuranceType: "Business Insurance", 



    const stopAutoAdvance = () => {                      {testimonial.verified && (    verified: true

      if (autoAdvance) {

        clearInterval(autoAdvance);                        <div class="absolute -bottom-1 -right-1 bg-green-500 rounded-full p-1">  }

      }

    };                          <svg class="w-3 h-3 text-white" fill="currentColor" viewBox="0 0 20 20">];



    const carousel = document.getElementById('testimonialCarousel');                            <path d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"/>---

    if (carousel) {

      carousel.addEventListener('mouseenter', stopAutoAdvance);                          </svg>

      carousel.addEventListener('mouseleave', startAutoAdvance);

    }                        </div><section class="py-20 bg-gradient-to-b from-blue-50/50 to-white dark:from-blue-950/20 dark:to-gray-900 relative overflow-hidden">



    startAutoAdvance();                      )}  <!-- Background Elements -->



    // Keyboard navigation                    </div>  <div class="absolute inset-0 opacity-20">

    document.addEventListener('keydown', (e) => {

      if (e.key === 'ArrowLeft') prevSlide();                    <div>    <div class="absolute top-1/4 left-10 w-20 h-20 bg-blue-400/30 rounded-full blur-2xl animate-float-testimonial-1"></div>

      if (e.key === 'ArrowRight') nextSlide();

    });                      <div class="font-semibold text-gray-900 dark:text-white">    <div class="absolute bottom-1/4 right-16 w-16 h-16 bg-purple-400/30 rounded-full blur-xl animate-float-testimonial-2"></div>

  });

</script>                        {testimonial.name}    <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-32 h-32 bg-indigo-300/20 rounded-full blur-3xl animate-pulse-testimonial"></div>

                      </div>  </div>

                      <div class="text-sm text-gray-500 dark:text-gray-400">

                        {testimonial.role} • {testimonial.location}  <div class="container mx-auto px-4 relative z-10">

                      </div>    <!-- Header -->

                    </div>    <div class="text-center mb-16">

                    <div class="ml-auto flex items-center text-green-600 dark:text-green-400 text-sm font-medium">      <div class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-green-100 to-blue-100 dark:from-green-900/50 dark:to-blue-900/50 rounded-full border border-green-200/50 dark:border-green-700/50 text-sm font-medium text-green-700 dark:text-green-300 mb-6">

                      <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">        <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">

                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>          <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>

                      </svg>        </svg>

                      Saved {testimonial.savings}        Trusted by 100K+ Users

                    </div>      </div>

                  </div>      

                </div>      <h2 class="text-4xl lg:text-5xl font-bold bg-gradient-to-r from-gray-900 to-blue-900 dark:from-white dark:to-blue-200 bg-clip-text text-transparent mb-6">

              </div>        Real Stories from

            </div>        <span class="block bg-gradient-to-r from-green-600 to-blue-600 bg-clip-text text-transparent">

          ))}          Real Customers

        </div>        </span>

      </div>      </h2>

      

      <!-- Navigation Arrows -->      <p class="text-xl text-gray-600 dark:text-gray-300 max-w-3xl mx-auto leading-relaxed">

      <button id="prevBtn" class="absolute left-4 top-1/2 transform -translate-y-1/2 w-12 h-12 bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm rounded-full shadow-lg border border-gray-200/50 dark:border-gray-700/50 flex items-center justify-center text-gray-600 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 hover:scale-110 transition-all duration-300 z-10">        See how Sezarr has helped thousands save money and find better coverage across all insurance types

        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">      </p>

          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>    </div>

        </svg>

      </button>    <!-- Testimonials Carousel -->

    <div class="relative max-w-6xl mx-auto">

      <button id="nextBtn" class="absolute right-4 top-1/2 transform -translate-y-1/2 w-12 h-12 bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm rounded-full shadow-lg border border-gray-200/50 dark:border-gray-700/50 flex items-center justify-center text-gray-600 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 hover:scale-110 transition-all duration-300 z-10">      <!-- Carousel Container -->

        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">      <div id="testimonialCarousel" class="overflow-hidden">

          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>        <div class="flex transition-transform duration-700 ease-in-out" id="testimonialTrack">

        </svg>          {testimonials.map((testimonial, index) => (

      </button>            <div class="w-full flex-shrink-0 px-4" data-testimonial={index}>

              <div class="group bg-white/80 dark:bg-gray-800/80 backdrop-blur-md rounded-3xl p-8 lg:p-10 shadow-xl border border-white/50 dark:border-gray-700/50 hover:shadow-2xl hover:scale-[1.02] transition-all duration-500">

      <!-- Pagination Dots -->                <!-- Quote Icon -->

      <div class="flex justify-center mt-12 space-x-3" id="paginationDots">                <div class="flex justify-center mb-6">

        {testimonials.map((_, index) => (                  <div class="w-16 h-16 bg-gradient-to-r from-blue-500 to-purple-500 rounded-full flex items-center justify-center shadow-lg group-hover:scale-110 transition-transform duration-300">

          <button                    <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 24 24">

            class="pagination-dot rounded-full transition-all duration-300 hover:scale-125"                      <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h4v10h-10z"/>

            data-slide={index}                    </svg>

            aria-label={`Go to testimonial ${index + 1}`}                  </div>

            style={`width:44px;height:44px;min-width:44px;min-height:44px;display:inline-flex;align-items:center;justify-center;padding:0;margin:0 4px;border-radius:50%;border:none;background:#e5e7eb;cursor:pointer;outline:none;touch-action:manipulation;`}                </div>

            tabIndex="0"

          >                <!-- Star Rating -->

            <span class="sr-only">Go to testimonial {index + 1}</span>                <div class="flex justify-center mb-6">

          </button>                  <div class="flex space-x-1">

        ))}                    {Array.from({ length: testimonial.rating }).map((_, i) => (

      </div>                      <svg key={i} class="w-5 h-5 text-yellow-400 animate-star-glow" fill="currentColor" viewBox="0 0 20 20" style={`animation-delay: ${i * 100}ms`}>

                        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>

      <div class="grid grid-cols-3 gap-8 mt-12">                      </svg>

        <div class="flex flex-col items-center">                    ))}

          <div class="text-3xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent mb-2">4.9★</div>                  </div>

          <div class="text-sm text-gray-600 dark:text-gray-300">Average Rating</div>                </div>

        </div>

        <div class="flex flex-col items-center">                <!-- Testimonial Text -->

          <div class="text-3xl font-bold bg-gradient-to-r from-orange-600 to-red-600 bg-clip-text text-transparent mb-2">$627</div>                <blockquote class="text-lg lg:text-xl text-gray-700 dark:text-gray-300 leading-relaxed text-center mb-8 italic">

          <div class="text-sm text-gray-600 dark:text-gray-300">Average Savings</div>                  "{testimonial.text}"

        </div>                </blockquote>

        <div class="flex flex-col items-center">

          <div class="text-3xl font-bold bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent mb-2">2M+</div>                <!-- User Info -->

          <div class="text-sm text-gray-600 dark:text-gray-300">Quotes Generated</div>                <div class="flex flex-col items-center space-y-4">

        </div>                  <!-- Avatar -->

      </div>                  <div class="relative">

    </div>                    <div class="w-20 h-20 bg-gradient-to-r from-blue-400 to-purple-400 rounded-full flex items-center justify-center text-white text-2xl font-bold shadow-lg">

  </div>                      {testimonial.name.split(' ').map(n => n[0]).join('')}

</section>                    </div>

                    {testimonial.verified && (

<style>                      <div class="absolute -bottom-1 -right-1 w-6 h-6 bg-green-500 rounded-full flex items-center justify-center border-2 border-white dark:border-gray-800">

  /* Testimonial-specific animations */                        <svg class="w-3 h-3 text-white" fill="currentColor" viewBox="0 0 20 20">

  @keyframes float-testimonial-1 {                          <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>

    0%, 100% { transform: translateY(0px) rotate(0deg); }                        </svg>

    50% { transform: translateY(-15px) rotate(3deg); }                      </div>

  }                    )}

                  </div>

  @keyframes float-testimonial-2 {

    0%, 100% { transform: translateY(0px) rotate(0deg); }                  <!-- Name and Role -->

    50% { transform: translateY(12px) rotate(-3deg); }                  <div class="text-center">

  }                    <h3 class="text-xl font-bold text-gray-900 dark:text-white">

                      {testimonial.name}

  @keyframes pulse-testimonial {                    </h3>

    0%, 100% { opacity: 0.2; transform: scale(1); }                    <p class="text-gray-600 dark:text-gray-400">

    50% { opacity: 0.4; transform: scale(1.05); }                      {testimonial.role} • {testimonial.location}

  }                    </p>

                  </div>

  @keyframes star-glow {

    0%, 100% { filter: brightness(1); }                  <!-- Insurance Type and Savings -->

    50% { filter: brightness(1.2) drop-shadow(0 0 2px currentColor); }                  <div class="flex flex-col sm:flex-row items-center gap-4 pt-4 border-t border-gray-200 dark:border-gray-700 w-full justify-center">

  }                    <div class="flex items-center text-sm">

                      <span class="px-3 py-1 bg-blue-100 dark:bg-blue-900/50 text-blue-700 dark:text-blue-300 rounded-full font-medium">

  /* Animation Classes */                        {testimonial.insuranceType}

  .animate-float-testimonial-1 {                      </span>

    animation: float-testimonial-1 6s ease-in-out infinite;                    </div>

  }                    <div class="flex items-center text-sm font-bold text-green-600 dark:text-green-400">

                      <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20">

  .animate-float-testimonial-2 {                        <path d="M8.433 7.418c.155-.103.346-.196.567-.267v1.698a2.305 2.305 0 01-.567-.267C8.07 8.34 8 8.114 8 8c0-.114.07-.34.433-.582zM11 12.849v-1.698c.22.071.412.164.567.267.364.243.433.468.433.582 0 .114-.07.34-.433.582a2.305 2.305 0 01-.567.267z"/>

    animation: float-testimonial-2 8s ease-in-out infinite;                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-13a1 1 0 10-2 0v.092a4.535 4.535 0 00-1.676.662C6.602 6.234 6 7.009 6 8c0 .99.602 1.765 1.324 2.246.48.32 1.054.545 1.676.662v1.941c-.391-.127-.68-.317-.843-.504a1 1 0 10-1.51 1.31c.562.649 1.413 1.076 2.353 1.253V15a1 1 0 102 0v-.092a4.535 4.535 0 001.676-.662C13.398 13.766 14 12.991 14 12c0-.99-.602-1.765-1.324-2.246A4.535 4.535 0 0011 9.092V7.151c.391.127.68.317.843.504a1 1 0 101.511-1.31c-.563-.649-1.413-1.076-2.354-1.253V5z" clip-rule="evenodd"/>

  }                      </svg>

                      const testimonials = [
  .animate-star-glow {            </div>

    animation: star-glow 2s ease-in-out infinite;          ))}

  }        </div>

      </div>

  /* Pagination Dots */

  .pagination-dot {      <!-- Navigation Arrows -->

    background: rgba(156, 163, 175, 0.5);      <button id="prevBtn" class="absolute left-4 top-1/2 transform -translate-y-1/2 w-12 h-12 bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm rounded-full shadow-lg border border-gray-200/50 dark:border-gray-700/50 flex items-center justify-center text-gray-600 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 hover:scale-110 transition-all duration-300 z-10">

    width: 44px;        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">

    height: 44px;          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>

    margin: 0 6px;        </svg>

    min-width: 44px;      </button>

    min-height: 44px;

    display: inline-flex;      <button id="nextBtn" class="absolute right-4 top-1/2 transform -translate-y-1/2 w-12 h-12 bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm rounded-full shadow-lg border border-gray-200/50 dark:border-gray-700/50 flex items-center justify-center text-gray-600 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 hover:scale-110 transition-all duration-300 z-10">

    align-items: center;        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">


    border: none;        </svg>

    cursor: pointer;      </button>

  }

      <!-- Pagination Dots -->

  .pagination-dot.active {      <div class="flex justify-center mt-12 space-x-3" id="paginationDots">

    background: linear-gradient(45deg, #3B82F6, #8B5CF6);        {testimonials.map((_, index) => (

    transform: scale(1.25);          <button

  }            class={`pagination-dot rounded-full transition-all duration-300 hover:scale-125 ${index === currentIndex ? 'active' : ''}`}

            data-slide={index}

  /* Accessibility - Respect motion preferences */            aria-label={`Go to testimonial ${index + 1}`}

  @media (prefers-reduced-motion: reduce) {            onClick={() => goToSlide(index)}

    .animate-float-testimonial-1,            style={`width:44px;height:44px;min-width:44px;min-height:44px;display:inline-flex;align-items:center;justify-content:center;padding:0;margin:0 4px;border-radius:50%;border:none;background:${index === currentIndex ? '#2563eb' : '#e5e7eb'};cursor:pointer;outline:none;touch-action:manipulation;`}

    .animate-float-testimonial-2,            tabIndex="0"

    .animate-pulse-testimonial,          >

    .animate-star-glow {            <span class="sr-only">Go to testimonial {index + 1}</span>

      animation: none;          </button>

    }        ))}

  }        <div class="flex flex-col items-center">

          <div class="text-3xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent mb-2">4.9★</div>

  /* Mobile Optimizations */          <div class="text-sm text-gray-600 dark:text-gray-300">Average Rating</div>

  @media (max-width: 768px) {        </div>

    #prevBtn,        <div class="flex flex-col items-center">

    #nextBtn {          <div class="text-3xl font-bold bg-gradient-to-r from-orange-600 to-red-600 bg-clip-text text-transparent mb-2">$627</div>

      display: none;          <div class="text-sm text-gray-600 dark:text-gray-300">Average Savings</div>

    }        </div>

            <div class="flex flex-col items-center">

    #testimonialCarousel {          <div class="text-3xl font-bold bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent mb-2">2M+</div>

      padding: 0 1rem;          <div class="text-sm text-gray-600 dark:text-gray-300">Quotes Generated</div>

    }        </div>

  }      </div>

</style>    </div>

  </div>

<script></section>

  document.addEventListener('DOMContentLoaded', () => {

    const track = document.getElementById('testimonialTrack');<style>

    const prevBtn = document.getElementById('prevBtn');  /* Testimonial-specific animations */

    const nextBtn = document.getElementById('nextBtn');  @keyframes float-testimonial-1 {

    const dots = document.querySelectorAll('.pagination-dot');    0%, 100% { transform: translateY(0px) rotate(0deg); }

    const totalSlides = document.querySelectorAll('[data-testimonial]').length;    50% { transform: translateY(-15px) rotate(3deg); }

    let currentIndex = 0;  }



    function updateSlidePosition() {  @keyframes float-testimonial-2 {

      if (track) {    0%, 100% { transform: translateY(0px) rotate(0deg); }

        track.style.transform = `translateX(-${currentIndex * 100}%)`;    50% { transform: translateY(12px) rotate(-3deg); }

        dots.forEach((dot, index) => {  }

          dot.style.background = index === currentIndex ? 'linear-gradient(45deg, #3B82F6, #8B5CF6)' : '#e5e7eb';

          dot.style.transform = index === currentIndex ? 'scale(1.25)' : 'scale(1)';  @keyframes pulse-testimonial {

        });    0%, 100% { opacity: 0.2; transform: scale(1); }

      }    50% { opacity: 0.4; transform: scale(1.05); }

    }  }



    function nextSlide() {  @keyframes star-glow {

      currentIndex = (currentIndex + 1) % totalSlides;    0%, 100% { filter: brightness(1); }

      updateSlidePosition();    50% { filter: brightness(1.2) drop-shadow(0 0 2px currentColor); }

    }  }



    function prevSlide() {  /* Animation Classes */

      currentIndex = (currentIndex - 1 + totalSlides) % totalSlides;  .animate-float-testimonial-1 {

      updateSlidePosition();    animation: float-testimonial-1 6s ease-in-out infinite;

    }  }



    function goToSlide(index) {  .animate-float-testimonial-2 {

      currentIndex = Math.max(0, Math.min(index, totalSlides - 1));    animation: float-testimonial-2 8s ease-in-out infinite;

      updateSlidePosition();  }

    }

  .animate-pulse-testimonial {

    // Event Listeners    animation: pulse-testimonial 4s ease-in-out infinite;

    if (prevBtn) prevBtn.addEventListener('click', prevSlide);  }

    if (nextBtn) nextBtn.addEventListener('click', nextSlide);

      .animate-star-glow {

    dots.forEach((dot, index) => {    animation: star-glow 2s ease-in-out infinite;

      dot.addEventListener('click', () => goToSlide(index));  }

    });

  /* Pagination Dots */

    // Initialize  .pagination-dot {

    updateSlidePosition();    background: rgba(156, 163, 175, 0.5);

    width: 44px;

    // Auto-advance slides every 5 seconds if not hovering    height: 44px;

    let autoAdvance;    margin: 0 6px;

    const startAutoAdvance = () => {    min-width: 44px;

      autoAdvance = setInterval(nextSlide, 5000);    min-height: 44px;

    };    display: inline-flex;

    align-items: center;

    const stopAutoAdvance = () => {    justify-content: center;

      if (autoAdvance) {    border: none;

        clearInterval(autoAdvance);    cursor: pointer;

      }  }

    };

  .pagination-dot.active {

    const carousel = document.getElementById('testimonialCarousel');    background: linear-gradient(45deg, #3B82F6, #8B5CF6);

    if (carousel) {    transform: scale(1.25);

      carousel.addEventListener('mouseenter', stopAutoAdvance);  }

      carousel.addEventListener('mouseleave', startAutoAdvance);

    }  /* Accessibility - Respect motion preferences */

  @media (prefers-reduced-motion: reduce) {

    startAutoAdvance();    .animate-float-testimonial-1,

    .animate-float-testimonial-2,

    // Keyboard navigation    .animate-pulse-testimonial,

    document.addEventListener('keydown', (e) => {    .animate-star-glow {

      if (e.key === 'ArrowLeft') prevSlide();      animation: none;

      if (e.key === 'ArrowRight') nextSlide();    }

    });  }

  });

</script>  /* Mobile Optimizations */
  @media (max-width: 768px) {
    #prevBtn,
    #nextBtn {
      display: none;
    }
    
    ---
    ---
    // Minimal valid Astro component for build recovery
    ---

    <section>
      <h2>Testimonials Carousel Component Restored</h2>
      <p>This component is now clean and ready for incremental feature restoration.</p>
    </section>

    
    dots.forEach((dot, index) => {
      dot.addEventListener('click', () => goToSlide(index));
    });

    // Initialize
    updateSlidePosition();

    // Auto-advance slides every 5 seconds if not hovering
    let autoAdvance;
    const startAutoAdvance = () => {
      autoAdvance = setInterval(nextSlide, 5000);
    };

    const stopAutoAdvance = () => {
      if (autoAdvance) {
        clearInterval(autoAdvance);
      }
    };

    const carousel = document.getElementById('testimonialCarousel');
    if (carousel) {
      carousel.addEventListener('mouseenter', stopAutoAdvance);
      carousel.addEventListener('mouseleave', startAutoAdvance);
    }

    startAutoAdvance();

    // Keyboard navigation
    document.addEventListener('keydown', (e) => {
      if (e.key === 'ArrowLeft') prevSlide();
      if (e.key === 'ArrowRight') nextSlide();
    });
  });
</script>

<script>
  let currentIndex = 0;
  const totalSlides = document.querySelectorAll('[data-testimonial]').length;
  const track = document.getElementById('testimonialTrack');
  const prevBtn = document.getElementById('prevBtn');
  const nextBtn = document.getElementById('nextBtn');
  const dots = document.querySelectorAll('.pagination-dot');

  function updateSlidePosition() {
    if (track) {
      track.style.transform = `translateX(-${currentIndex * 100}%)`;
      dots.forEach((dot, index) => {
        dot.style.background = index === currentIndex ? 'linear-gradient(45deg, #3B82F6, #8B5CF6)' : '#e5e7eb';
        dot.style.transform = index === currentIndex ? 'scale(1.25)' : 'scale(1)';
      });
    }
  }

  function goToSlide(index) {
    currentIndex = Math.max(0, Math.min(index, totalSlides - 1));
    updateSlidePosition();
  }

  function nextSlide() {
    currentIndex = (currentIndex + 1) % totalSlides;
    updateSlidePosition();
  }

  function prevSlide() {
    currentIndex = (currentIndex - 1 + totalSlides) % totalSlides;
    updateSlidePosition();
  }

  // Initialize carousel
  document.addEventListener('DOMContentLoaded', () => {
    if (prevBtn) prevBtn.addEventListener('click', prevSlide);
    if (nextBtn) nextBtn.addEventListener('click', nextSlide);
    dots.forEach((dot, index) => {
      dot.addEventListener('click', () => goToSlide(index));
    });
    updateSlidePosition();

    // Auto-advance slides every 5 seconds
    setInterval(nextSlide, 5000);
  });

  // Keyboard navigation
  document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowLeft') prevSlide();
    if (e.key === 'ArrowRight') nextSlide();
  });
</script>